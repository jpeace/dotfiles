alias vt="vim -c 'NERDTree'"
alias be="bundle exec"
alias ber="bundle exec rake"

rnew() { rails new $@ --git --database=postgresql --skip-bundle; } 

alias sudo="sudo env PATH=$PATH"

function npm-do { (PATH=$(npm bin):$PATH; eval $@;) }
